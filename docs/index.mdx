---
title: Welcome
description: Distributed Claude AI agents on Kubernetes — first-class cluster resources via CRDs and operators.
---

import { Card, Cards } from 'fumadocs-ui/components/card';
import { Rocket, Code2, Boxes, Users, Plug, FileText, BookOpen, BarChart3, Calendar, DollarSign, Network, Radio, Container, Activity, ScrollText, Layers, Sparkles, Brain } from 'lucide-react';

**komputer.ai** is a Kubernetes-native platform for running persistent Claude AI agents as first-class cluster resources. No external database — `KomputerAgent` CRs live in etcd, the operator reconciles them into Pods, and the API streams every event back over WebSocket.

<video
  src="/komputer-ai/agent-page-video.mp4"
  autoPlay
  loop
  muted
  playsInline
  controls
  className="my-8 w-full rounded-lg border border-fd-border shadow-lg"
/>

## Quick start

```bash
helm install komputer-ai oci://ghcr.io/komputer-ai/charts/komputer-ai \
  --set anthropicKeySecret.name=anthropic-api-key \
  --namespace komputer-ai --create-namespace
```

That deploys the operator, API, UI, Redis, and a default agent template. From there: port-forward, open the dashboard, create your first agent. Full walkthrough in [Installation](/docs/getting-started/installation).

## Get started

<Cards>
  <Card icon={<Rocket />} title="Installation" href="/docs/getting-started/installation" description="Helm install on any Kubernetes cluster — five minutes from zero to running agent." />
  <Card icon={<Network />} title="Architecture" href="/docs/architecture" description="Components, data flow, protocols, tech stack, and the design choices behind the platform." />
  <Card icon={<Code2 />} title="Integration overview" href="/docs/integration/overview" description="Drive komputer.ai from your own services via REST + WebSocket." />
</Cards>

## Core concepts

<Cards>
  <Card icon={<Boxes />} title="Agents" href="/docs/concepts/agents" description="The central entity — lifecycle modes, roles, overrides, concurrency control." />
  <Card icon={<Users />} title="Squads" href="/docs/concepts/squads" description="Multi-agent shared workspaces via co-located Pods. Manager + worker collaboration." />
  <Card icon={<Layers />} title="Templates" href="/docs/concepts/templates" description="Reusable Pod specs for agents — image, resources, storage, service account." />
  <Card icon={<Plug />} title="Connectors" href="/docs/concepts/connectors" description="MCP server connections per agent or template; includes the supported-services compatibility matrix." />
</Cards>

## Power features

<Cards>
  <Card icon={<Sparkles />} title="Skills" href="/docs/concepts/skills" description="Reusable capabilities attached to agents as Kubernetes CRs — written into the workspace at start." />
  <Card icon={<Brain />} title="Memories" href="/docs/concepts/memories" description="Persistent knowledge injected into the system prompt. Per-agent or per-namespace." />
  <Card icon={<Calendar />} title="Schedules" href="/docs/concepts/schedules" description="Cron-based recurring tasks with timezone support and auto-cleanup." />
  <Card icon={<DollarSign />} title="Cost tracking" href="/docs/concepts/cost-tracking" description="Real-time per-task cost, context window monitoring, per-agent breakdown with task-level drill-down." />
</Cards>

## Build with it

<Cards>
  <Card icon={<Code2 />} title="REST API" href="/docs/integration/rest-api" description="Create agents, send tasks, list status, fetch results — full control plane in HTTP." />
  <Card icon={<Radio />} title="WebSocket events" href="/docs/integration/websocket" description="Stream tokens, tool calls, costs, and lifecycle transitions in real time as agents work." />
  <Card icon={<Container />} title="Custom agent images" href="/docs/integration/custom-agent-image" description="Bake your own packages, CLIs, or runtimes into the agent image." />
</Cards>

## Operate it

<Cards>
  <Card icon={<Activity />} title="Monitoring" href="/docs/observability/monitoring" description="Prometheus metrics, ServiceMonitors, and a Grafana dashboard ship with the chart." />
  <Card icon={<ScrollText />} title="Logging" href="/docs/observability/logging" description="Structured JSON or human-readable logs across all components." />
</Cards>

## Why komputer.ai

- **Kubernetes-native** — agents are CRDs reconciled by an operator. Pods, PVCs, lifecycle, GitOps — all standard primitives.
- **Stateless API** — no database. CR `.status` is the source of truth; Redis is just transport.
- **Real-time streaming** — REST for control, WebSocket for live token-by-token output, tool calls, and costs.
- **Squads & sub-agents** — co-located multi-agent pods. Manager agents create and orchestrate workers via MCP tools.
- **Built-in observability** — structured logs, Prometheus metrics, and a Grafana dashboard ship with the chart.
- **MCP connectors** — plug in MCP servers per agent or template; secrets and lifecycle managed by the operator.
