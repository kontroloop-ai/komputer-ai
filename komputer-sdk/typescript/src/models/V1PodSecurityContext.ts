/* tslint:disable */
/* eslint-disable */
/**
 * komputer.ai API
 * API-first platform for running persistent Claude AI agents on Kubernetes. Designed to be driven by external systems — create agents, send tasks, and stream real-time results via REST + WebSocket.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { V1SELinuxOptions } from './V1SELinuxOptions';
import {
    V1SELinuxOptionsFromJSON,
    V1SELinuxOptionsFromJSONTyped,
    V1SELinuxOptionsToJSON,
    V1SELinuxOptionsToJSONTyped,
} from './V1SELinuxOptions';
import type { V1PodSELinuxChangePolicy } from './V1PodSELinuxChangePolicy';
import {
    V1PodSELinuxChangePolicyFromJSON,
    V1PodSELinuxChangePolicyFromJSONTyped,
    V1PodSELinuxChangePolicyToJSON,
    V1PodSELinuxChangePolicyToJSONTyped,
} from './V1PodSELinuxChangePolicy';
import type { V1SeccompProfile } from './V1SeccompProfile';
import {
    V1SeccompProfileFromJSON,
    V1SeccompProfileFromJSONTyped,
    V1SeccompProfileToJSON,
    V1SeccompProfileToJSONTyped,
} from './V1SeccompProfile';
import type { V1SupplementalGroupsPolicy } from './V1SupplementalGroupsPolicy';
import {
    V1SupplementalGroupsPolicyFromJSON,
    V1SupplementalGroupsPolicyFromJSONTyped,
    V1SupplementalGroupsPolicyToJSON,
    V1SupplementalGroupsPolicyToJSONTyped,
} from './V1SupplementalGroupsPolicy';
import type { V1WindowsSecurityContextOptions } from './V1WindowsSecurityContextOptions';
import {
    V1WindowsSecurityContextOptionsFromJSON,
    V1WindowsSecurityContextOptionsFromJSONTyped,
    V1WindowsSecurityContextOptionsToJSON,
    V1WindowsSecurityContextOptionsToJSONTyped,
} from './V1WindowsSecurityContextOptions';
import type { V1PodFSGroupChangePolicy } from './V1PodFSGroupChangePolicy';
import {
    V1PodFSGroupChangePolicyFromJSON,
    V1PodFSGroupChangePolicyFromJSONTyped,
    V1PodFSGroupChangePolicyToJSON,
    V1PodFSGroupChangePolicyToJSONTyped,
} from './V1PodFSGroupChangePolicy';
import type { V1AppArmorProfile } from './V1AppArmorProfile';
import {
    V1AppArmorProfileFromJSON,
    V1AppArmorProfileFromJSONTyped,
    V1AppArmorProfileToJSON,
    V1AppArmorProfileToJSONTyped,
} from './V1AppArmorProfile';
import type { V1Sysctl } from './V1Sysctl';
import {
    V1SysctlFromJSON,
    V1SysctlFromJSONTyped,
    V1SysctlToJSON,
    V1SysctlToJSONTyped,
} from './V1Sysctl';

/**
 * 
 * @export
 * @interface V1PodSecurityContext
 */
export interface V1PodSecurityContext {
    /**
     * appArmorProfile is the AppArmor options to use by the containers in this pod.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * @type {V1AppArmorProfile}
     * @memberof V1PodSecurityContext
     */
    appArmorProfile?: V1AppArmorProfile;
    /**
     * A special supplemental group that applies to all containers in a pod.
     * Some volume types allow the Kubelet to change the ownership of that volume
     * to be owned by the pod:
     * 
     * 1. The owning GID will be the FSGroup
     * 2. The setgid bit is set (new files created in the volume will be owned by FSGroup)
     * 3. The permission bits are OR'd with rw-rw----
     * 
     * If unset, the Kubelet will not modify the ownership and permissions of any volume.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * @type {number}
     * @memberof V1PodSecurityContext
     */
    fsGroup?: number;
    /**
     * fsGroupChangePolicy defines behavior of changing ownership and permission of the volume
     * before being exposed inside Pod. This field will only apply to
     * volume types which support fsGroup based ownership(and permissions).
     * It will have no effect on ephemeral volume types such as: secret, configmaps
     * and emptydir.
     * Valid values are "OnRootMismatch" and "Always". If not specified, "Always" is used.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * @type {V1PodFSGroupChangePolicy}
     * @memberof V1PodSecurityContext
     */
    fsGroupChangePolicy?: V1PodFSGroupChangePolicy;
    /**
     * The GID to run the entrypoint of the container process.
     * Uses runtime default if unset.
     * May also be set in SecurityContext.  If set in both SecurityContext and
     * PodSecurityContext, the value specified in SecurityContext takes precedence
     * for that container.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * @type {number}
     * @memberof V1PodSecurityContext
     */
    runAsGroup?: number;
    /**
     * Indicates that the container must run as a non-root user.
     * If true, the Kubelet will validate the image at runtime to ensure that it
     * does not run as UID 0 (root) and fail to start the container if it does.
     * If unset or false, no such validation will be performed.
     * May also be set in SecurityContext.  If set in both SecurityContext and
     * PodSecurityContext, the value specified in SecurityContext takes precedence.
     * +optional
     * @type {boolean}
     * @memberof V1PodSecurityContext
     */
    runAsNonRoot?: boolean;
    /**
     * The UID to run the entrypoint of the container process.
     * Defaults to user specified in image metadata if unspecified.
     * May also be set in SecurityContext.  If set in both SecurityContext and
     * PodSecurityContext, the value specified in SecurityContext takes precedence
     * for that container.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * @type {number}
     * @memberof V1PodSecurityContext
     */
    runAsUser?: number;
    /**
     * seLinuxChangePolicy defines how the container's SELinux label is applied to all volumes used by the Pod.
     * It has no effect on nodes that do not support SELinux or to volumes does not support SELinux.
     * Valid values are "MountOption" and "Recursive".
     * 
     * "Recursive" means relabeling of all files on all Pod volumes by the container runtime.
     * This may be slow for large volumes, but allows mixing privileged and unprivileged Pods sharing the same volume on the same node.
     * 
     * "MountOption" mounts all eligible Pod volumes with `-o context` mount option.
     * This requires all Pods that share the same volume to use the same SELinux label.
     * It is not possible to share the same volume among privileged and unprivileged Pods.
     * Eligible volumes are in-tree FibreChannel and iSCSI volumes, and all CSI volumes
     * whose CSI driver announces SELinux support by setting spec.seLinuxMount: true in their
     * CSIDriver instance. Other volumes are always re-labelled recursively.
     * "MountOption" value is allowed only when SELinuxMount feature gate is enabled.
     * 
     * If not specified and SELinuxMount feature gate is enabled, "MountOption" is used.
     * If not specified and SELinuxMount feature gate is disabled, "MountOption" is used for ReadWriteOncePod volumes
     * and "Recursive" for all other volumes.
     * 
     * This field affects only Pods that have SELinux label set, either in PodSecurityContext or in SecurityContext of all containers.
     * 
     * All Pods that use the same volume should use the same seLinuxChangePolicy, otherwise some pods can get stuck in ContainerCreating state.
     * Note that this field cannot be set when spec.os.name is windows.
     * +featureGate=SELinuxChangePolicy
     * +optional
     * @type {V1PodSELinuxChangePolicy}
     * @memberof V1PodSecurityContext
     */
    seLinuxChangePolicy?: V1PodSELinuxChangePolicy;
    /**
     * The SELinux context to be applied to all containers.
     * If unspecified, the container runtime will allocate a random SELinux context for each
     * container.  May also be set in SecurityContext.  If set in
     * both SecurityContext and PodSecurityContext, the value specified in SecurityContext
     * takes precedence for that container.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * @type {V1SELinuxOptions}
     * @memberof V1PodSecurityContext
     */
    seLinuxOptions?: V1SELinuxOptions;
    /**
     * The seccomp options to use by the containers in this pod.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * @type {V1SeccompProfile}
     * @memberof V1PodSecurityContext
     */
    seccompProfile?: V1SeccompProfile;
    /**
     * A list of groups applied to the first process run in each container, in
     * addition to the container's primary GID and fsGroup (if specified).  If
     * the SupplementalGroupsPolicy feature is enabled, the
     * supplementalGroupsPolicy field determines whether these are in addition
     * to or instead of any group memberships defined in the container image.
     * If unspecified, no additional groups are added, though group memberships
     * defined in the container image may still be used, depending on the
     * supplementalGroupsPolicy field.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * +listType=atomic
     * @type {Array<number>}
     * @memberof V1PodSecurityContext
     */
    supplementalGroups?: Array<number>;
    /**
     * Defines how supplemental groups of the first container processes are calculated.
     * Valid values are "Merge" and "Strict". If not specified, "Merge" is used.
     * (Alpha) Using the field requires the SupplementalGroupsPolicy feature gate to be enabled
     * and the container runtime must implement support for this feature.
     * Note that this field cannot be set when spec.os.name is windows.
     * TODO: update the default value to "Merge" when spec.os.name is not windows in v1.34
     * +featureGate=SupplementalGroupsPolicy
     * +optional
     * @type {V1SupplementalGroupsPolicy}
     * @memberof V1PodSecurityContext
     */
    supplementalGroupsPolicy?: V1SupplementalGroupsPolicy;
    /**
     * Sysctls hold a list of namespaced sysctls used for the pod. Pods with unsupported
     * sysctls (by the container runtime) might fail to launch.
     * Note that this field cannot be set when spec.os.name is windows.
     * +optional
     * +listType=atomic
     * @type {Array<V1Sysctl>}
     * @memberof V1PodSecurityContext
     */
    sysctls?: Array<V1Sysctl>;
    /**
     * The Windows specific settings applied to all containers.
     * If unspecified, the options within a container's SecurityContext will be used.
     * If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
     * Note that this field cannot be set when spec.os.name is linux.
     * +optional
     * @type {V1WindowsSecurityContextOptions}
     * @memberof V1PodSecurityContext
     */
    windowsOptions?: V1WindowsSecurityContextOptions;
}



/**
 * Check if a given object implements the V1PodSecurityContext interface.
 */
export function instanceOfV1PodSecurityContext(value: object): value is V1PodSecurityContext {
    return true;
}

export function V1PodSecurityContextFromJSON(json: any): V1PodSecurityContext {
    return V1PodSecurityContextFromJSONTyped(json, false);
}

export function V1PodSecurityContextFromJSONTyped(json: any, ignoreDiscriminator: boolean): V1PodSecurityContext {
    if (json == null) {
        return json;
    }
    return {
        
        'appArmorProfile': json['appArmorProfile'] == null ? undefined : V1AppArmorProfileFromJSON(json['appArmorProfile']),
        'fsGroup': json['fsGroup'] == null ? undefined : json['fsGroup'],
        'fsGroupChangePolicy': json['fsGroupChangePolicy'] == null ? undefined : V1PodFSGroupChangePolicyFromJSON(json['fsGroupChangePolicy']),
        'runAsGroup': json['runAsGroup'] == null ? undefined : json['runAsGroup'],
        'runAsNonRoot': json['runAsNonRoot'] == null ? undefined : json['runAsNonRoot'],
        'runAsUser': json['runAsUser'] == null ? undefined : json['runAsUser'],
        'seLinuxChangePolicy': json['seLinuxChangePolicy'] == null ? undefined : V1PodSELinuxChangePolicyFromJSON(json['seLinuxChangePolicy']),
        'seLinuxOptions': json['seLinuxOptions'] == null ? undefined : V1SELinuxOptionsFromJSON(json['seLinuxOptions']),
        'seccompProfile': json['seccompProfile'] == null ? undefined : V1SeccompProfileFromJSON(json['seccompProfile']),
        'supplementalGroups': json['supplementalGroups'] == null ? undefined : json['supplementalGroups'],
        'supplementalGroupsPolicy': json['supplementalGroupsPolicy'] == null ? undefined : V1SupplementalGroupsPolicyFromJSON(json['supplementalGroupsPolicy']),
        'sysctls': json['sysctls'] == null ? undefined : ((json['sysctls'] as Array<any>).map(V1SysctlFromJSON)),
        'windowsOptions': json['windowsOptions'] == null ? undefined : V1WindowsSecurityContextOptionsFromJSON(json['windowsOptions']),
    };
}

export function V1PodSecurityContextToJSON(json: any): V1PodSecurityContext {
    return V1PodSecurityContextToJSONTyped(json, false);
}

export function V1PodSecurityContextToJSONTyped(value?: V1PodSecurityContext | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'appArmorProfile': V1AppArmorProfileToJSON(value['appArmorProfile']),
        'fsGroup': value['fsGroup'],
        'fsGroupChangePolicy': V1PodFSGroupChangePolicyToJSON(value['fsGroupChangePolicy']),
        'runAsGroup': value['runAsGroup'],
        'runAsNonRoot': value['runAsNonRoot'],
        'runAsUser': value['runAsUser'],
        'seLinuxChangePolicy': V1PodSELinuxChangePolicyToJSON(value['seLinuxChangePolicy']),
        'seLinuxOptions': V1SELinuxOptionsToJSON(value['seLinuxOptions']),
        'seccompProfile': V1SeccompProfileToJSON(value['seccompProfile']),
        'supplementalGroups': value['supplementalGroups'],
        'supplementalGroupsPolicy': V1SupplementalGroupsPolicyToJSON(value['supplementalGroupsPolicy']),
        'sysctls': value['sysctls'] == null ? undefined : ((value['sysctls'] as Array<any>).map(V1SysctlToJSON)),
        'windowsOptions': V1WindowsSecurityContextOptionsToJSON(value['windowsOptions']),
    };
}

