/* tslint:disable */
/* eslint-disable */
/**
 * komputer.ai API
 * API-first platform for running persistent Claude AI agents on Kubernetes. Designed to be driven by external systems — create agents, send tasks, and stream real-time results via REST + WebSocket.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface V1PodCertificateProjection
 */
export interface V1PodCertificateProjection {
    /**
     * Write the certificate chain at this path in the projected volume.
     * 
     * Most applications should use credentialBundlePath.  When using keyPath
     * and certificateChainPath, your application needs to check that the key
     * and leaf certificate are consistent, because it is possible to read the
     * files mid-rotation.
     * 
     * +optional
     * @type {string}
     * @memberof V1PodCertificateProjection
     */
    certificateChainPath?: string;
    /**
     * Write the credential bundle at this path in the projected volume.
     * 
     * The credential bundle is a single file that contains multiple PEM blocks.
     * The first PEM block is a PRIVATE KEY block, containing a PKCS#8 private
     * key.
     * 
     * The remaining blocks are CERTIFICATE blocks, containing the issued
     * certificate chain from the signer (leaf and any intermediates).
     * 
     * Using credentialBundlePath lets your Pod's application code make a single
     * atomic read that retrieves a consistent key and certificate chain.  If you
     * project them to separate files, your application code will need to
     * additionally check that the leaf certificate was issued to the key.
     * 
     * +optional
     * @type {string}
     * @memberof V1PodCertificateProjection
     */
    credentialBundlePath?: string;
    /**
     * Write the key at this path in the projected volume.
     * 
     * Most applications should use credentialBundlePath.  When using keyPath
     * and certificateChainPath, your application needs to check that the key
     * and leaf certificate are consistent, because it is possible to read the
     * files mid-rotation.
     * 
     * +optional
     * @type {string}
     * @memberof V1PodCertificateProjection
     */
    keyPath?: string;
    /**
     * The type of keypair Kubelet will generate for the pod.
     * 
     * Valid values are "RSA3072", "RSA4096", "ECDSAP256", "ECDSAP384",
     * "ECDSAP521", and "ED25519".
     * 
     * +required
     * @type {string}
     * @memberof V1PodCertificateProjection
     */
    keyType?: string;
    /**
     * maxExpirationSeconds is the maximum lifetime permitted for the
     * certificate.
     * 
     * Kubelet copies this value verbatim into the PodCertificateRequests it
     * generates for this projection.
     * 
     * If omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver
     * will reject values shorter than 3600 (1 hour).  The maximum allowable
     * value is 7862400 (91 days).
     * 
     * The signer implementation is then free to issue a certificate with any
     * lifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600
     * seconds (1 hour).  This constraint is enforced by kube-apiserver.
     * `kubernetes.io` signers will never issue certificates with a lifetime
     * longer than 24 hours.
     * 
     * +optional
     * @type {number}
     * @memberof V1PodCertificateProjection
     */
    maxExpirationSeconds?: number;
    /**
     * Kubelet's generated CSRs will be addressed to this signer.
     * 
     * +required
     * @type {string}
     * @memberof V1PodCertificateProjection
     */
    signerName?: string;
    /**
     * userAnnotations allow pod authors to pass additional information to
     * the signer implementation.  Kubernetes does not restrict or validate this
     * metadata in any way.
     * 
     * These values are copied verbatim into the `spec.unverifiedUserAnnotations` field of
     * the PodCertificateRequest objects that Kubelet creates.
     * 
     * Entries are subject to the same validation as object metadata annotations,
     * with the addition that all keys must be domain-prefixed. No restrictions
     * are placed on values, except an overall size limitation on the entire field.
     * 
     * Signers should document the keys and values they support. Signers should
     * deny requests that contain keys they do not recognize.
     * @type {{ [key: string]: string; }}
     * @memberof V1PodCertificateProjection
     */
    userAnnotations?: { [key: string]: string; };
}

/**
 * Check if a given object implements the V1PodCertificateProjection interface.
 */
export function instanceOfV1PodCertificateProjection(value: object): value is V1PodCertificateProjection {
    return true;
}

export function V1PodCertificateProjectionFromJSON(json: any): V1PodCertificateProjection {
    return V1PodCertificateProjectionFromJSONTyped(json, false);
}

export function V1PodCertificateProjectionFromJSONTyped(json: any, ignoreDiscriminator: boolean): V1PodCertificateProjection {
    if (json == null) {
        return json;
    }
    return {
        
        'certificateChainPath': json['certificateChainPath'] == null ? undefined : json['certificateChainPath'],
        'credentialBundlePath': json['credentialBundlePath'] == null ? undefined : json['credentialBundlePath'],
        'keyPath': json['keyPath'] == null ? undefined : json['keyPath'],
        'keyType': json['keyType'] == null ? undefined : json['keyType'],
        'maxExpirationSeconds': json['maxExpirationSeconds'] == null ? undefined : json['maxExpirationSeconds'],
        'signerName': json['signerName'] == null ? undefined : json['signerName'],
        'userAnnotations': json['userAnnotations'] == null ? undefined : json['userAnnotations'],
    };
}

export function V1PodCertificateProjectionToJSON(json: any): V1PodCertificateProjection {
    return V1PodCertificateProjectionToJSONTyped(json, false);
}

export function V1PodCertificateProjectionToJSONTyped(value?: V1PodCertificateProjection | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'certificateChainPath': value['certificateChainPath'],
        'credentialBundlePath': value['credentialBundlePath'],
        'keyPath': value['keyPath'],
        'keyType': value['keyType'],
        'maxExpirationSeconds': value['maxExpirationSeconds'],
        'signerName': value['signerName'],
        'userAnnotations': value['userAnnotations'],
    };
}

